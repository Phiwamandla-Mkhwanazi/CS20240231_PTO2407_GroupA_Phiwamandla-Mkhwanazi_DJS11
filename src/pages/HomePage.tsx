import Main from '../components/Main';  // Import the Main component which contains the homepage content/layout

// Home component serves as the root page of the application
function Home() {
  return (
        <Main />      // Render the Main component    
  );
}

export default Home;  // Export the Home component as default