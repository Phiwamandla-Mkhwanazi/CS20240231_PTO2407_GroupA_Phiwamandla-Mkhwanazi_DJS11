import Guide from '../components/Guide';  // Import the Guide component which provides user instructions or help content

// Home component that renders the Guide section
function Home() {
  return (
        <Guide />    // Render the Guide component
  );
}
// Export the Home component as the default export
export default Home;